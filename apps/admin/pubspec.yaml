name: budgetbuddy_admin
description: Web admin panel for BudgetBuddy Social.
publish_to: none
version: 0.1.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  budgetbuddy_shared:
    path: ../../packages/budgetbuddy_shared
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.7
  supabase_flutter: ^2.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  lints: ^4.0.0

flutter:
  uses-material-design: true
